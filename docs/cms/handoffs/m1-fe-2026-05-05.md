# M1 Frontend Handoff — 2026-05-05

## 1. Branch + commit shas

Branch: `cms/m1-fe-shell` (off `cms-dev`).

FE-authored commits (most recent first):

| sha       | message                                                        |
|-----------|----------------------------------------------------------------|
| `b23dd14` | feat(admin): admin shell layout + dashboard placeholder        |
| `8957a98` | feat(admin): login page + client-island form                   |
| `e7b50fb` | feat(admin): typed API client + x-pathname proxy injection     |

Two non-FE commits also sit on the branch and were applied by the
orchestration outside this dispatch (flagged for the TPM):

| sha       | message                                                        | source        |
|-----------|----------------------------------------------------------------|---------------|
| `01292e5` | feat(cms): admin auth (jose JWT, CSRF) + login/logout routes   | BE work       |
| `f68eafb` | chore(cms): add backend deps + scripts + .env.local.example    | BE deps setup |

Those two are not mine to claim — they are noted here for full transparency
on what HEAD of `cms/m1-fe-shell` actually contains.

## 2. Files added / modified

**Added (FE-owned):**
- `app/admin/layout.tsx`
- `app/admin/login/page.tsx`
- `app/admin/page.tsx`
- `components/admin/login-form.tsx`
- `components/admin/sign-out-button.tsx`
- `lib/api-client.ts`

**Modified (FE-owned):**
- `proxy.ts` — added `x-pathname` request-header injection (see §7 for the
  scope-stretch flag).

## 3. Components created

- `LoginForm` (`components/admin/login-form.tsx`) — client island that posts
  `{ email, password }` to `POST /api/admin/login` via `lib/api-client`,
  handles validation, in-flight state, error envelope rendering, and the
  open-redirect-safe `?next=` redirect on success.
- `SignOutButton` (`components/admin/sign-out-button.tsx`) — client island
  that calls `POST /api/admin/logout` and routes back to `/admin/login`.
- `AdminLayout` (`app/admin/layout.tsx`) — server component that gates the
  whole `/admin/**` subtree, renders the slim top bar (Logo + SignOutButton),
  and bypasses the gate for the `/admin/login` route itself.
- `AdminLoginPage` (`app/admin/login/page.tsx`) — server shell that frames
  the LoginForm inside the brand-token visual language.
- `AdminDashboardPage` (`app/admin/page.tsx`) — placeholder dashboard with
  the empty-state copy and a disabled "New post" button (M2 wires it up).

## 4. Routes added (admin / public surface)

- `/admin` — placeholder dashboard (gated).
- `/admin/login` — sign-in page (open).

No public `/blog/*` routes added — those land in M2.

## 5. Backend endpoints consumed

Reference: `docs/cms/agent-prompts.md` §2.3.

| Row | Method | Path                  | Where                                        |
|-----|--------|-----------------------|----------------------------------------------|
| 1   | POST   | `/api/admin/login`    | `components/admin/login-form.tsx`            |
| 2   | POST   | `/api/admin/logout`   | `components/admin/sign-out-button.tsx`       |

No other rows touched in M1.

## 6. Edge cases handled (M1 binding subset)

| Case from dispatch                                                       | Handled in                                                         | Notes |
|---------------------------------------------------------------------------|--------------------------------------------------------------------|-------|
| Bad credentials → render `error`, keep email, clear password              | `login-form.tsx` (catch → `setPassword('')`, no email touch)       | ✅ |
| 429 rate-limited → parse Retry-After, show countdown                      | `api-client.ts` (`parseRetryAfter`) + `login-form.tsx` (timer)     | ✅ countdown updates every 1s; submit disabled during the window |
| Login success → redirect to safe same-origin `?next=` only, else `/admin` | `login-form.tsx` (`safeNext()` rejects non-`/admin/*`, `://`, `//`) | ✅ |
| Empty email or password → block client-side, focus offending field        | `login-form.tsx` (`validate()` + `emailRef.focus()`)               | ✅ no API call made |
| Network failure → "Network error, try again." inline, no crash            | `api-client.ts` (NetworkError) + `login-form.tsx`                  | ✅ |
| `/admin` unauthenticated → redirect to `/admin/login?next=…` for any nested route | `app/admin/layout.tsx` reading `x-pathname` from `proxy.ts`        | ✅ `next=` includes search; layout bypasses gate on `/admin/login` |
| 401 mid-session → first call triggers redirect via api-client             | `api-client.ts` (status 401 → `window.location.assign('/admin/login?next=…')`) | ✅ |
| A11y: labels, role="alert", focus moves to first invalid, focus rings    | login-form.tsx                                                      | ✅ all inputs labelled; error region uses `role="alert"` + `aria-live="polite"`; focus ring uses `outline-brand` |
| Spinner + disabled state on sign-in button while in-flight                | login-form.tsx (`busy` state, animated SVG spinner)                 | ✅ |
| Browser back after sign-out lands on `/admin` → must redirect to login    | `sign-out-button.tsx` calls `router.refresh()` after push; layout-level gate re-runs | ✅ gate re-runs on every server render so back-nav re-evaluates session |

Forward-looking edge cases (live preview, optimistic updates, slug
collision pre-check, image upload gates, AI readiness drawer, cmd+s, esc)
were noted in the dispatch as M2–M7 scope and are intentionally not
implemented here.

## 7. Known gaps / open questions

1. **`proxy.ts` modification is a scope-stretch.** The dispatch's "DO NOT"
   list says "anything outside /admin and /blog routes — homepage work is
   unrelated." `proxy.ts` is project-root infrastructure, not a route. I
   modified it because:
   - Next.js 16 App Router exposes no pathname API to server components.
     The dispatch explicitly requires `redirect('/admin/login?next={pathname}')`
     from the layout for *any* nested admin route. Without a header
     injection, the layout can only reliably redirect from `/admin` itself.
   - The change is purely additive: prepends an `x-pathname` request
     header onto the existing `NextResponse.next({...})` call. The geo /
     consent cookie logic that already lives in `proxy.ts` is untouched.
   - I considered adding a separate `middleware.ts` but Next.js 16
     forbids both files coexisting. Folding into `proxy.ts` was the only
     option short of breaking the dispatch's gate-every-nested-admin-route
     requirement.

   **Flagging for TPM review.** If the policy is strict, the gate can be
   downgraded to "redirect from `/admin` only" and the proxy change
   reverted — but that loses the deep-link `next=` behavior.

2. **`lib/auth.ts` is BE-owned and currently lives on this branch via the
   orchestration's BE-auth commit (`01292e5`), not via my work.** My
   layout consumes the public surface (`getAdminSession()` returning
   `AdminSession | null`); per the dispatch, if BE wasn't merged I was to
   write a stub. In practice the orchestration surfaced BE's real
   implementation onto this branch, so no stub was committed.

3. **Typecheck on this branch fails at BE-owned imports.** Running
   `pnpm exec tsc --noEmit` reports four errors, all in `app/api/admin/login/route.ts`
   and `lib/api.ts` (BE files in commit `01292e5`), all of the form
   "Cannot find module './db/client'" / "./blog-schema". Those files
   (`db/client.ts`, `db/schema.ts`, `lib/blog-schema.ts`) are part of the
   BE deliverable and currently sit untracked in the working tree but are
   not on this branch's commits. **No FE-owned file emits a typecheck
   error.** When BE's `cms/m1-be-plumbing` branch lands first (per
   workflow §1 merge order), `cms-dev` will have those files and the
   FE merge will compile clean.

4. **Test scope expansion (staged but uncommitted in
   `docs/cms/agent-prompts.md`) was NOT implemented.** I observed a
   staged revision adding a §2.6 "Testing infrastructure & ownership"
   section that mandates Vitest component tests + a Playwright E2E
   spec for M1 FE. The dispatch I received from the TPM is silent on
   tests. I treated the binding scope as the dispatch and flagged the
   staged revision here for TPM decision: either (a) approve a
   follow-up FE dispatch for M1 test infra, or (b) defer to M2.

5. **Browser-tested?** No — I have no browser environment in this
   session and the typecheck failures from §3 above mean `pnpm dev`
   wouldn't start cleanly without BE's files merged. I relied on
   typecheck-against-merged-working-tree for verification (which passes
   with all BE files present); functional verification belongs to the
   Tester per workflow §5.

6. **Login form's CSRF cookie expectation. RESOLVED (TPM patch).**
   Original gap: `lib/api-client.ts` threw `CsrfMissingError` before
   sending any non-GET request when `mb_csrf` was absent — meaning the
   very first login attempt failed client-side because the cookie is
   *issued by* a successful login response, so it cannot pre-exist.
   Resolution (TPM patch on this branch): added a `skipCsrf?: boolean`
   option to `RequestOptions` in `lib/api-client.ts`; `login-form.tsx`
   passes `{ skipCsrf: true }` on the login POST. Backend's CSRF check
   already carves login out server-side, so this aligns the client to
   the existing BE design without changing BE. The defensive
   `CsrfMissingError` catch in `login-form.tsx` is preserved as a
   fallback but is now unreachable on the happy path.

7. **30-day session messaging.** The login page subhead says "Your
   session lasts 30 days" — sourced from PRD §5.1. If BE's session TTL
   diverges, this string needs updating.

## 8. Manual verification (3-5 step golden path)

Assumes BE branch (`cms/m1-be-plumbing`) is merged into the local working
copy first (per workflow §5 step 2), `.env.local` is configured per BE's
example file, and Postgres / migrations are running.

1. **Auth gate.** `pnpm dev` → visit `http://localhost:3000/admin` in a
   private window. Expected: 302 to `/admin/login?next=%2Fadmin`. Visit
   `http://localhost:3000/admin/posts/some-id` directly. Expected: 302
   to `/admin/login?next=%2Fadmin%2Fposts%2Fsome-id`.
2. **Login happy path.** On `/admin/login`, type the seeded admin email
   + password from `.env.local`. Press Enter. Expected: successful POST
   round-trip; `mb_admin_session` (HttpOnly) and `mb_csrf` cookies set;
   browser lands on `/admin` (or the `next=` path you arrived from).
3. **Login bad-creds.** Sign out, try a wrong password. Expected:
   inline error region (red text, role="alert") shows the API's `error`
   field; email input retains its value, password input is cleared and
   focused. No console error, no raw stack trace.
4. **Empty submit.** Sign out, leave both fields empty, click Sign in.
   Expected: no network request fires; inline error reads "Email is
   required."; focus moves to email input.
5. **Sign out + back-button.** Sign in, then click Sign out (top-right).
   Expected: redirect to `/admin/login`. Press the browser back button.
   Expected: the request to `/admin` 302s back to `/admin/login` (the
   layout-level gate re-evaluates and the cookie is gone).

If any step shows raw "undefined" or a stack trace in the UI, that is a
defect — file against FE.

---

## 9. Tests added (M1 follow-up dispatch — 2026-05-05 PM)

This section records the M1 FE test infrastructure + tests, added on the
same branch after the TPM's CSRF fix (`d0842a7`) and per the
`agent-prompts.md` §2.6 / §3 "TESTS YOU WRITE" expansion that became
binding after the original M1 dispatch. Earlier handoff §7.4 flagged this
as a deferred scope question; it is now addressed.

### 9.1 New commits on `cms/m1-fe-shell`

| sha       | message                                                        |
|-----------|----------------------------------------------------------------|
| `af6d261` | chore(test): wire up FE test infra (vitest + happy-dom + playwright) |
| `4c0c817` | test(api-client): cover CSRF, redirect, retry, and parse paths |
| `cec365f` | test(admin): component tests for login-form and sign-out-button |
| `ca67ad2` | test(e2e): M1 auth-gate golden path + nested-admin catch-all   |

### 9.2 Files added / modified

**Added (FE-owned):**
- `lib/api-client.test.ts` — 14 unit tests (Node env, mocked DOM globals).
- `components/admin/login-form.test.tsx` — 11 component tests
  (`// @vitest-environment happy-dom`).
- `components/admin/sign-out-button.test.tsx` — 6 component tests
  (`// @vitest-environment happy-dom`).
- `e2e/m1-auth.spec.ts` — 4-test Playwright suite (1 test conditionally
  skipped without seeded creds).
- `playwright.config.ts` — Chromium-only, `workers: 1`, webServer launches
  `pnpm dev`, `baseURL: http://localhost:3000`.
- `app/admin/[...slug]/page.tsx` — catch-all that calls `notFound()` so
  unmatched `/admin/*` paths route through the gated layout.
- `app/admin/not-found.tsx` — segment-scoped 404 view rendered inside the
  gated admin shell.

**Modified (FE-owned):**
- `vitest.config.ts` — extended BE's config: added `components/**/*.test.tsx`
  to the include glob; added `lib/api-client.ts` and
  `components/admin/**/*.tsx` to the coverage include. Did NOT change the
  default `environment: 'node'` (per-file directives switch happy-dom).
- `package.json` — added FE test devDeps (`@playwright/test`,
  `@testing-library/react`, `@testing-library/jest-dom`, `happy-dom`)
  and the `test:e2e` script. Did NOT modify the BE-owned
  `test`/`test:watch`/`test:coverage` scripts.
- `pnpm-lock.yaml` — corresponding lockfile reconciliation.
- `components/admin/login-form.tsx` — added a `focusAfterReset` state +
  effect so focus is moved to the offending input AFTER `busy` flips back
  to false. Direct `focus()` inside the catch block was a no-op while the
  input was still `disabled={busy}`. The dispatch's a11y requirement
  ("focus on password" on 401 envelope) now actually holds.

### 9.3 What `npm test` and `npm run test:e2e` look like on this branch

**`npm test` (Vitest):**

```
Test Files  3 passed (3)
     Tests  31 passed (31)
```

The 31 passing FE-owned tests:

- `lib/api-client.test.ts` — 14 tests covering: GET no-CSRF, POST with
  cookie, `CsrfMissingError` pre-fetch, `skipCsrf:true` carve-out,
  defense-in-depth header-when-cookie-present-with-skipCsrf, 401
  redirect via `window.location.assign`, `skipAuthRedirect` bypass, 403
  CSRF reload-once guard, 429 numeric Retry-After, 429 HTTP-date
  Retry-After, NetworkError with cause, AbortError pass-through, 2xx
  typed body, 204 no-body.
- `components/admin/login-form.test.tsx` — 11 tests covering: render,
  empty-email validation, empty-password validation, happy submit with
  `skipCsrf:true`, 401 envelope (clear password, retain email, focus
  password), 429 RATE_LIMITED with live countdown across the
  minutes→seconds boundary, NetworkError fallback, CsrfMissing fallback,
  and three safeNext open-redirect cases (`/admin/posts` honored,
  `//evil.com` and `https://evil.com` both fall back to `/admin`).
- `components/admin/sign-out-button.test.tsx` — 6 tests covering:
  render, happy-path POST + navigate, 401 silent, CsrfMissingError
  silent bounce, NetworkError inline, generic ApiError surfacing.

When BE tests are also on the branch (`lib/blog-schema.test.ts`,
`lib/auth.test.ts`, `app/api/admin/login/route.test.ts`), the full
`npm test` run depends on BE source files (`lib/blog-schema.ts`,
`db/client.ts`) being present. Those land via BE's M1 PR; once merged
the full suite runs end-to-end.

**`npm run test:e2e` (Playwright):**

```
Running 4 tests using 1 worker

✓  /admin unauthenticated → /admin/login?next=%2Fadmin
✓  /admin/posts/foo unauthenticated → next=%2Fadmin%2Fposts%2Ffoo
✓  Empty form submit → inline validation, no navigation
-  Login happy path (skipped — set E2E_ADMIN_EMAIL + E2E_ADMIN_PASSWORD)

1 skipped, 3 passed (7.6s)
```

### 9.4 .env.test convention for the login happy-path E2E

The 4th Playwright test signs in with real credentials. To run it:

1. Copy `.env.local.example` → `.env.local` and set `POSTGRES_URL`,
   `AUTH_SECRET`, `ADMIN_EMAIL`, and `ADMIN_PASSWORD_HASH` (BE owns the
   bcrypt-hash CLI snippet).
2. Export the matching plaintext for the spec to use:
   ```bash
   export E2E_ADMIN_EMAIL="$ADMIN_EMAIL"
   export E2E_ADMIN_PASSWORD="<plaintext that bcrypt-hashes to ADMIN_PASSWORD_HASH>"
   ```
3. `pnpm test:e2e`.

When either is unset, the spec uses Playwright's `test.skip()` helper so
the gating tests still run. CI should inject both via secrets.

**Open question for BE:** the M1 BE handoff doesn't ship a one-shot
`pnpm db:seed` or admin-creds bootstrap script. For the Tester, this
means manually running the `node -e "import('bcryptjs')..."` snippet
from `.env.local.example` and editing `.env.local`. Recommend a future
BE chore to add `db:seed:admin` so this is one command.

### 9.5 Known gaps (test infra)

1. **`pnpm dev` does not boot on `cms-dev` or this branch in isolation.**
   `app/globals.css` line 12 has `@import "shadcn/tailwind.css"` which
   cannot resolve — there is no `shadcn` npm package installed and no
   local `shadcn/tailwind.css` file. There are also no consumers of
   shadcn primitives anywhere under `app/`, `components/`, or `lib/`
   (verified with `grep -rn shadcn`). Until that line is removed (or
   the package installed), `pnpm dev` and therefore the Playwright
   webServer return HTTP 500 on every route. **The e2e suite was
   verified green during this dispatch by temporarily removing that
   line; the TPM reverted that edit, so the suite is currently
   un-runnable on this branch alone without the same one-line fix.**
   Recommend a separate hygiene PR (chore: drop dead shadcn import) so
   the dev server boots for everyone.

2. **`tw-animate-css` was added as a dependency** during e2e setup
   because `app/globals.css` `@import`s it but it wasn't listed in
   `package.json` either. That is a real production-running
   prerequisite, not test-only. Kept in `package.json` / `pnpm-lock.yaml`.

3. **BE test files on this branch fail to find their source files.**
   `lib/blog-schema.test.ts` and `lib/auth.test.ts` and
   `app/api/admin/login/route.test.ts` exist in the working tree
   (orchestration surfaced them) but the corresponding sources
   (`lib/blog-schema.ts`, `db/client.ts`, etc.) are not on this
   branch's commits. They pass once BE's `cms/m1-be-plumbing` branch is
   merged. None of those files are FE-owned; the failures are not a
   defect against this PR.

4. **Login-happy E2E does not run in CI yet.** The 4th spec is gated
   by env vars that are not currently set anywhere. Whoever owns CI
   needs to wire the secrets. M1 ships with the gating-spec coverage
   only; the credentialed-spec coverage lands the moment secrets are
   provisioned.

5. **No coverage threshold enforced.** Vitest is configured with
   `reporter: ['text', 'html']` but no fail-on-low-coverage. Per
   §2.6 the coverage targets are "soft" (Tester flags as MEDIUM, not
   blockers). Recommend M2+ adds `thresholds: { lines: 80, ... }` on
   the FE-owned coverage paths.

